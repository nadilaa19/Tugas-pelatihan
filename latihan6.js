// 1. Define the regex pattern with the case-insensitive flag
const myRegex = /skilvul/i;

// 2. Define the string to search within
const words = "Saya belajar coding di Skilvul setiap hari";

// 3. Perform the regex match
const result = words.match(myRegex);

// 4. Output the result
console.log(result);
